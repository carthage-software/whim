namespace Whim\Filesystem;

use Whim\Unwind\InvalidArgumentException;

/** Reports a path that does not point to a symbolic link. */
final class NotSymbolicLinkException extends InvalidArgumentException {}
