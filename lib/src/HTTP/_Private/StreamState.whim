namespace Whim\HTTP\_Private;

/** An HTTP/2 stream state. */
enum StreamState {
  case Idle;
  case ReservedLocal;
  case ReservedRemote;
  case Open;
  case HalfClosedLocal;
  case HalfClosedRemote;
  case Closed;
}
