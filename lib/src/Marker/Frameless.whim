namespace Whim\Marker;

use Whim\Attribute\Attribute;

/** Marks an eligible zero-parameter literal-returning callable that executes without a frame. */
#[Stub]
#[Attribute(Attribute::TARGET_FUNCTION | Attribute::TARGET_METHOD)]
final class Frameless {}
