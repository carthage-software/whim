namespace Whim\Command;

use Whim\Unwind\UnsupportedPlatformException;

/** Reports a signal the running system does not name. */
final class UnsupportedSignalException extends UnsupportedPlatformException {}
